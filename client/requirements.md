## Packages
recharts | Dashboard charts and analytics
framer-motion | Smooth transitions and animations
lucide-react | Icons for the UI (already in base, but emphasizing usage)
date-fns | Date formatting

## Notes
Authentication uses JWT stored in localStorage (handled by use-auth.tsx)
Dashboard requires aggregations for stats
Reports are CSV downloads
